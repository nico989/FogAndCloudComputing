apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storedb
  labels:
    app: backend
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
